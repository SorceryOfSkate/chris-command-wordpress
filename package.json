{
  "name": "chris-command-wordpress",
  "version": "0.2.0",
  "private": true,
  "description": "Development tooling for the Chris Command WordPress plugin.",
  "scripts": {
    "boundary": "pwsh -NoProfile -File tools/scan-public-boundary.ps1",
    "build:release": "pwsh -NoProfile -File tools/build-release.ps1",
    "verify:release": "pwsh -NoProfile -File tools/verify-release.ps1"
  },
  "devDependencies": {
    "@wp-playground/cli": "3.1.51",
    "@wordpress/env": "11.14.0"
  }
}
